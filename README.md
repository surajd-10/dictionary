# dictionary
Simple application which fetches a dummy data from an open-source API and display it in the form of table. Application also has ability add new records and delete existing records. Written in JavaScript, HTML and CSS. 
